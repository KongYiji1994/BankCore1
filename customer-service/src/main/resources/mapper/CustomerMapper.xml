<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bankcore.customer.repository.CustomerMapper">
    <resultMap id="CustomerMap" type="com.bankcore.customer.model.Customer">
        <id column="customer_id" property="customerId"/>
        <result column="name" property="name"/>
        <result column="credit_code" property="creditCode"/>
        <result column="contact_name" property="contactName"/>
        <result column="contact_phone" property="contactPhone"/>
        <result column="onboard_date" property="onboardDate"/>
        <result column="risk_level" property="riskLevel"/>
        <result column="status" property="status"/>
        <result column="segment" property="segment"/>
    </resultMap>

    <select id="findAll" resultMap="CustomerMap">
        SELECT customer_id, name, credit_code, contact_name, contact_phone, onboard_date, risk_level, status, segment FROM customers
    </select>

    <select id="findById" parameterType="string" resultMap="CustomerMap">
        SELECT customer_id, name, credit_code, contact_name, contact_phone, onboard_date, risk_level, status, segment FROM customers WHERE customer_id = #{id}
    </select>

    <insert id="insert" parameterType="com.bankcore.customer.model.Customer">
        INSERT INTO customers(customer_id, name, credit_code, contact_name, contact_phone, onboard_date, risk_level, status, segment)
        VALUES(#{customerId}, #{name}, #{creditCode}, #{contactName}, #{contactPhone}, #{onboardDate}, #{riskLevel}, #{status}, #{segment})
    </insert>
</mapper>
