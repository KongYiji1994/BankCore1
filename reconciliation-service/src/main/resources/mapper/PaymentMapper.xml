<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bankcore.settlement.repository.PaymentMapper">

    <resultMap id="PaymentRecordMap" type="com.bankcore.settlement.model.PaymentRecord">
        <id property="instructionId" column="instruction_id" />
        <result property="payerAccount" column="payer_account" />
        <result property="payeeAccount" column="payee_account" />
        <result property="amount" column="amount" />
        <result property="currency" column="currency" />
        <result property="status" column="status" />
        <result property="createdAt" column="created_at" />
    </resultMap>

    <select id="findPaymentsForDate" parameterType="java.time.LocalDate" resultMap="PaymentRecordMap">
        SELECT instruction_id, payer_account, payee_account, amount, currency, status, created_at
        FROM payments
        WHERE DATE(created_at) = #{reconDate}
    </select>
</mapper>
